class Destination {
  String description, url, phone, address,pid,locName, state, latitude, longitude, imagename;

  Destination(
      {this.description,
      this.url,
      this.phone,
      this.address, pid, locName, state, latitude, longitude, imagename,
      });
}